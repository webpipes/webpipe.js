#!/usr/bin/env node

require("coffee-script");

var path = require('path');
var fs   = require('fs');
var src  = path.join(path.dirname(fs.realpathSync(__filename)), '../src');

require(src + '/webpipe-cli').run();
